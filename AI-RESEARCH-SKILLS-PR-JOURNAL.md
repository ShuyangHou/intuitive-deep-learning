# AI Research Skills PR 开发记录

## 2026-07-27｜Loss Guide 本科课堂化改造

- 开发者：侯舒扬
- 分支：`feat/loss-guide-classroom`
- 目标：将原有的损失函数入门演示扩展为面向具备高数、线代和概率基础的计算机专业本科生的 `45 + 15` 分钟课堂模块。

### 主要改动

- 将课程扩展为单样本误差、数据集 reduction、离群点实验、梯度分析、损失函数选择六个核心环节。
- 增加可跳过的概率建模拓展，说明高斯噪声与 MSE、拉普拉斯噪声与 MAE 的联系。
- 在计算 MAE 和 MSE 前展示统一公式与符号约定，并集中实现可复用的损失计算函数。
- 增加课堂节奏、教学难度、适用对象、可选模块等课程元数据。
- 为数轴、离群点实验、题目和概率拓展增加版本化进度恢复，避免旧版学习记录干扰新版流程。
- 增加各教学区块的独立开发预览路由，并补充响应式与键盘可访问性样式。

### 验证记录

- `npm run typecheck`：通过。
- `npm run build`：通过。
- SSR 首屏渲染、版本化课程进度键、MAE/MSE 示例数值与空数据边界：通过。
- `git diff --check`：通过。

### 说明

- 构建过程中仍会提示 `modules/shared/telemetry.js` 未声明 `type="module"`；该提示为已有构建配置产生，不影响本次构建成功。
- 本次构思、实现检查和提交前审查使用了 Codex 辅助，最终教学内容与代码由开发者确认。

## 2026-07-28｜课堂深度第二轮扩充

- 将离群点实验由“有/无异常值”切换改为连续误差滑杆，同步观察 MAE、MSE 与两者梯度信号的变化。
- 在损失函数求导之后加入线性模型参数更新演练，用链式法则把 `∂L/∂ŷ` 传递到 `∂L/∂w`，并完成一次可核算的梯度下降更新。
- 增加 Huber 损失交互模块，通过误差与阈值 `δ` 比较二次区间、线性尾部和梯度截断效果。
- 将概率拓展改为“条件密度 → 独立样本联合似然 → 负对数似然 → 高斯/拉普拉斯噪声”的逐步推导。
- 补充 MAE、MSE、Huber 的任务选择矩阵，并把核心课时重新分配为 8 个连续环节、合计 45 分钟。
